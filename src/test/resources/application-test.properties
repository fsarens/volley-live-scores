# Use a fast-failing URI so the MongoClient background monitor doesn't block the build
# Repositories are mocked so no actual MongoDB operations happen
spring.mongodb.uri=mongodb://localhost:27017/test?serverSelectionTimeoutMS=200&connectTimeoutMS=200
